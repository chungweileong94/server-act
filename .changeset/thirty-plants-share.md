---
"server-act": minor
---

`serverAct.formAction` has been removed.

Use `serverAct.stateAction` instead, and update handler payload names from `formData` to `rawInput` and from `formErrors` to `inputErrors`.
