{
  "name": "@tests/e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:fixtures": "vp run server-act#build && vp run --parallel --filter @examples/nextjs --filter @examples/react-router build",
    "pretest:e2e": "vp run @tests/e2e#build:fixtures",
    "test:e2e": "playwright test",
    "pretest:e2e:ui": "vp run @tests/e2e#build:fixtures",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.13.3"
  }
}
